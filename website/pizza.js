let currentPizza = newPizza();
let currentOrder = newPizzaOrder();

function startNewOrder() {

}

function startNewPizza() {

}

/**
 * Add the current pizza to the order.
 * Create a new pizza to start customizing.
 * Update the UI to reflect the order updates.
 */
function addCurrentPizzaToOrder() {
  console.log("addCurrentPizzaToOrder");
}


/**
 * Add this new topping to the current pizza.
 * @param {*String} topping 
 */
function addTopping(topping) {
  console.log(topping);
}

/**
 * Change the current pizza's size.
 * @param {*String} size 
 */
function changeSize(size) {
  console.log(size);
}